---
title: Dialog
---

import { TypeTable } from "fumadocs-ui/components/type-table";

A modal dialog element that renders in a portal overlay. Extends Group functionality with open/close state management.

## Constructor

```csharp
new Dialog(
    new DialogContent(
        new Label("Dialog Title"),
        new Label("Dialog content goes here")
    )
);
```

## Examples

### Controlled Dialog
Dialogs are controlled by ReactiveProperty:

```csharp
// Dialog bound to state
new Dialog(
    new DialogContent(
        new Label("Confirm Delete"),
        new Label("Are you sure you want to delete this item?"),
        new HorizontalGroup(
            new Button(new Label("Cancel"))
                .OnClick(_ => CloseConfirmDialog()),
            new Button(new Label("Delete"))
                .OnClick(_ => ConfirmDelete())
        )
    )
)
.BindOpen(ShowConfirmDialog);
```

### Form Dialog
Use dialogs for complex forms with validation:

```csharp
new Dialog(
    new DialogContent(
        new Label("Edit User"),
        new TextField(EditingUser.Name)
            .Placeholder("Name"),
        new TextField(EditingUser.Email)
            .Placeholder("Email"),
        new HorizontalGroup(
            new Button(new Label("Cancel"))
                .OnClick(_ => CancelEdit()),
            new Button(new Label("Save"))
                .OnClick(_ => SaveUser())
                .BindDisabled(IsFormValid.Select(v => !v))
        )
    )
)
.BindOpen(ShowEditDialog)
.OnClose(() => CancelEdit());
```

### Component Dialog
Dialogs can render sub-components:

```csharp
new Dialog(
    new DialogContent(
        new Label("User List"),
        new UserList(SelectedUser)
    )
)
.BindOpen(ShowUserList);
```

## Properties

<TypeTable
  type={{
    children: {
      description: "Array of child elements contained within the dialog",
      type: "IElement[]",
      default: "[]",
    },
    open: {
      description: "Whether the dialog is currently open",
      type: "bool",
      default: "false",
    },
    disabled: {
      description: "Whether the dialog is disabled",
      type: "bool",
      default: "false",
    },
    visible: {
      description: "Whether the dialog is visible",
      type: "bool",
      default: "true",
    },
    name: {
      description: "The name identifier for the dialog",
      type: "string",
      default: '""',
    },
    pickingMode: {
      description: "How the dialog responds to pointer events",
      type: "PickingMode",
      default: "PickingMode.Position",
    }
  }}
/>

## Methods

<TypeTable
  type={{
    Open: {
      description: "Opens the dialog",
      type: "(bool = true) => Dialog",
    },
    Close: {
      description: "Closes the dialog",
      type: "() => Dialog",
    },
    GetOpen: {
      description: "Gets the open state",
      type: "() => bool",
    },
    BindOpen: {
      description: "Binds open state to an observable",
      type: "(Observable<bool>) => Dialog",
    },
    OnClose: {
      description: "Registers a callback for when dialog closes",
      type: "(Action) => Dialog",
    },
    Disabled: {
      description: "Sets the disabled state",
      type: "(bool = true) => Dialog",
    },
    GetDisabled: {
      description: "Gets the disabled state",
      type: "() => bool",
    },
    BindDisabled: {
      description: "Binds disabled state to an observable",
      type: "(Observable<bool>) => Dialog",
    },
    Visible: {
      description: "Sets the visibility state",
      type: "(bool) => Dialog",
    },
    GetVisible: {
      description: "Gets the visibility state",
      type: "() => bool",
    },
    BindVisible: {
      description: "Binds visibility to an observable",
      type: "(Observable<bool>) => Dialog",
    },
    ClassName: {
      description: "Adds or removes a CSS class",
      type: "(string, bool = true) => Dialog",
    },
    BindClassName: {
      description: "Binds CSS class to an observable",
      type: "(string, Observable<bool>) => Dialog",
    },
    StyleSheet: {
      description: "Adds a StyleSheet to the dialog",
      type: "(StyleSheet) => Dialog or (string) => Dialog",
    },
    Flex: {
      description: "Sets flex grow and shrink values",
      type: "(int flexGrow, int flexShrink = 0) => Dialog",
    },
    JustifyContent: {
      description: "Sets how children are distributed along the main axis",
      type: "(Justify) => Dialog",
    },
    AlignItems: {
      description: "Sets how children are aligned along the cross axis",
      type: "(Align) => Dialog",
    },
    BindChildren: {
      description: "Binds children to an observable array with converter function",
      type: "<TChild>(Observable<TChild[]>, Func<TChild, IElement>) => Dialog",
    }
  }}
/>

Dialog extends [Group](/docs/elements/group) which extends [BaseElement](/docs/elements/base-element).
