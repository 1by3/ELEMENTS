VisualElement {
    color: #0f172a;
}

VisualElement,
Button,
Label {
    margin: 0;
    padding: 0;
}

Label {
    white-space: normal;
}

TextField {
    padding: 0;
    margin: 0;
}

.unity-text-field__placeholder > .unity-base-text-field__input {
    color: #94a3b8;
}

TextField TextInput {
    padding: 8px;
    border-radius: 8px;
    border-color: #e2e8f0;
    border-width: 1px;
    background-color: #ffffff;
    height: auto;
    transition-property: background-color, border-color, border-left-width, border-right-width, border-top-width, border-bottom-width;
    transition-duration: 0.2s;
}

TextField:focus TextInput {
    border-color: #64748b;
}

Button {
    flex-direction: row;
    align-items: center;
    justify-content: center;
    padding: 8px 16px;
    border-radius: 8px;
    border-color: #e2e8f0;
    border-width: 1px;
    background-color: #ffffff;
    height: auto;
    transition-property: background-color, border-color;
    transition-duration: 0.2s;
}

Button:hover {
    background-color: #f1f5f9;
}

Button.unity-disabled:hover {
    background-color: #ffffff;
}

Button Image {
    width: 16px;
    height: 16px;
    margin-right: 8px;
}

Button Image.rightIcon {
    margin-left: 8px;
    margin-right: 0;
}

Button.icon {
    padding: 8px;
    width: 34px;
}

Button.icon Image {
    margin-right: 0;
    margin-left: 0;
}

Button.ghost {
    background-color: rgba(255, 255, 255, 0);
    border-color: rgba(255, 255, 255, 0);
}

Button.ghost:hover {
    background-color: #f1f5f9;
}

Button.ghost.unity-disabled:hover {
    background-color: rgba(255, 255, 255, 0);
}

Button.pill {
    border-radius: 18px;
    background-color: #f1f5f9;
    border-color: #f1f5f9;
}

Button.pill:hover {
    background-color: #e2e8f0;
    border-color: #e2e8f0;
}

Button.pill.unity-disabled:hover {
    background-color: #f1f5f9;
    border-color: #f1f5f9;
}

Button.pill Label {
    color: #334155;
}

.tab-list .tab-item {
    border-width: 0;
    margin: 0 2px;
}

.tab-list .tab-item.first {
    margin-left: 0;
}

.tab-list .tab-item.last {
    margin-right: 0;
}

.elements-loader {
    width: 16px;
    height: 16px;
}

.elements-loader.large {
    width: 20px;
    height: 20px;
}

.elements-dialog {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background-color: rgba(0, 0, 0, 0);
    align-items: center;
    justify-content: center;
    display: none;
    visibility: hidden;

    transition-property: background-color;
    transition-duration: 0.2s;
}

.elements-dialog.open {
    display: flex;
    visibility: visible;
    background-color: rgba(0, 0, 0, 0.35);
}

.elements-dialog .elements-dialog-content {
    background-color: #ffffff;
    border-radius: 16px;
    padding: 32px;
    max-width: 500px;
    width: 100%;

    opacity: 0;
    scale: 0.8;
    transition-property: scale, opacity;
    transition-duration: 0.2s;
}

.elements-dialog.open .elements-dialog-content {
    scale: 1;
    opacity: 1;
}

.elements-alert Button {
    width: 100%;
}

.elements-alert .title {
    font-size: 16px;
    -unity-font-style: bold;
    margin-bottom: 8px;
}

.elements-alert .message {
    margin-bottom: 16px;
    color: #64748b;
}

/* Gap utilities — emulated via child margins + ELEMENTS pseudo-classes */

.gap-0 > * { margin-top: 0; margin-left: 0; }

.gap-1 > * { margin-top: 4px; margin-left: 4px; }
.gap-1 > .first-child { margin-top: 0; margin-left: 0; }

.gap-2 > * { margin-top: 8px; margin-left: 8px; }
.gap-2 > .first-child { margin-top: 0; margin-left: 0; }

.gap-3 > * { margin-top: 12px; margin-left: 12px; }
.gap-3 > .first-child { margin-top: 0; margin-left: 0; }

.gap-4 > * { margin-top: 16px; margin-left: 16px; }
.gap-4 > .first-child { margin-top: 0; margin-left: 0; }

.gap-5 > * { margin-top: 20px; margin-left: 20px; }
.gap-5 > .first-child { margin-top: 0; margin-left: 0; }

.gap-6 > * { margin-top: 24px; margin-left: 24px; }
.gap-6 > .first-child { margin-top: 0; margin-left: 0; }

.gap-8 > * { margin-top: 32px; margin-left: 32px; }
.gap-8 > .first-child { margin-top: 0; margin-left: 0; }

/* gap-y: vertical gap only */
.gap-y-0 > * { margin-top: 0; }

.gap-y-1 > * { margin-top: 4px; }
.gap-y-1 > .first-child { margin-top: 0; }

.gap-y-2 > * { margin-top: 8px; }
.gap-y-2 > .first-child { margin-top: 0; }

.gap-y-3 > * { margin-top: 12px; }
.gap-y-3 > .first-child { margin-top: 0; }

.gap-y-4 > * { margin-top: 16px; }
.gap-y-4 > .first-child { margin-top: 0; }

.gap-y-5 > * { margin-top: 20px; }
.gap-y-5 > .first-child { margin-top: 0; }

.gap-y-6 > * { margin-top: 24px; }
.gap-y-6 > .first-child { margin-top: 0; }

.gap-y-8 > * { margin-top: 32px; }
.gap-y-8 > .first-child { margin-top: 0; }

/* gap-x: horizontal gap only */
.gap-x-0 > * { margin-left: 0; }

.gap-x-1 > * { margin-left: 4px; }
.gap-x-1 > .first-child { margin-left: 0; }

.gap-x-2 > * { margin-left: 8px; }
.gap-x-2 > .first-child { margin-left: 0; }

.gap-x-3 > * { margin-left: 12px; }
.gap-x-3 > .first-child { margin-left: 0; }

.gap-x-4 > * { margin-left: 16px; }
.gap-x-4 > .first-child { margin-left: 0; }

.gap-x-5 > * { margin-left: 20px; }
.gap-x-5 > .first-child { margin-left: 0; }

.gap-x-6 > * { margin-left: 24px; }
.gap-x-6 > .first-child { margin-left: 0; }

.gap-x-8 > * { margin-left: 32px; }
.gap-x-8 > .first-child { margin-left: 0; }

/* Popover */
.elements-popover {
    position: absolute;
    display: none;
    opacity: 0;
    scale: 0.95;
    transition-property: opacity, scale;
    transition-duration: 0.15s;
    background-color: #ffffff;
    border-radius: 8px;
    border-width: 1px;
    border-color: #e2e8f0;
}

.elements-popover.open {
    display: flex;
    opacity: 1;
    scale: 1;
}

.elements-popover-backdrop {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background-color: transparent;
    display: none;
}

.elements-popover-backdrop.open {
    display: flex;
}

/* Context Menu Container */
.elements-context-menu {
    padding: 4px 0;
    min-width: 160px;
}

/* Menu Item */
.elements-menu-item {
    flex-direction: row;
    align-items: center;
    padding: 8px 12px;
    margin: 0 4px;
    border-radius: 4px;
    height: auto;
    color: #0f172a;
    background-color: rgba(255, 255, 255, 0);
    transition-property: background-color;
    transition-duration: 0.1s;
}

.elements-menu-item:hover {
    background-color: #f1f5f9;
}

.elements-menu-item.unity-disabled {
    opacity: 0.5;
}

.elements-menu-item.unity-disabled:hover {
    background-color: rgba(255, 255, 255, 0);
}

.elements-menu-item > Image {
    width: 16px;
    height: 16px;
    margin-right: 8px;
    flex-shrink: 0;
}

.elements-menu-item > Label {
    color: #0f172a;
    flex-grow: 1;
}

/* Menu Divider */
.elements-menu-divider {
    height: 1px;
    background-color: #e2e8f0;
    margin: 4px 12px;
}
